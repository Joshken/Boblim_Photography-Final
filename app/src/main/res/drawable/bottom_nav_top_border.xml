<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Top border only -->
    <item android:top="0dp" android:left="-9dp" android:right="-8dp" android:bottom="-8dp">
        <shape android:shape="rectangle">
            <stroke android:width="3dp" android:color="@color/burgundy" />
            <solid android:color="@color/white" />
        </shape>
    </item>
</layer-list>
